# 留言管理系统 — 架构报告

> 生成日期：2026-07-24  
> 版本：v1.0（全部 17 项优化已完成）  
> 用途：留档对比，后续迭代参考

---

## 一、项目概述

本项目为公司多站点（官网、阿里店铺等）提供统一的留言收集 API 和管理后台。

| 属性 | 值 |
|------|------|
| 项目名称 | 留言管理系统 (ReciveMessage) |
| 技术栈 | PHP 8.2 + SQLite 3 + 原生 HTML/JS/CSS |
| 架构模式 | 单入口 RESTful API + SPA 管理后台 |
| 数据库 | SQLite 3（WAL 模式），单文件 `data/messages.db` |
| 部署方式 | 独立子目录 + PHP 内置服务器或 Apache mod_php |

---

## 二、目录结构

```
ReciveMessage/
├── check.php                          # 部署检查 & 数据库初始化
├── 留言系统架构设计.md                  # 原始设计文档
├── 架构报告_20260724.md                # 本报告（实际实现版）
│
├── config/
│   └── database.php                   # 全局配置：DB路径、频率限制、CORS
│
├── src/                               # 核心类库（6个）
│   ├── Auth.php                       # 认证：API Key鉴权 + 用户登录/注销
│   ├── Database.php                   # PDO SQLite 封装（单例）
│   ├── Logger.php                     # JSON格式文件日志
│   ├── RateLimiter.php                # IP频率限制 + 防重复提交
│   ├── Response.php                   # 统一JSON响应 + CORS
│   └── Validator.php                  # 参数校验 + XSS过滤
│
├── api/                               # API 端点（8个）
│   ├── message.php                    # [公开] 留言提交
│   └── admin/
│       ├── login.php                  # [公开] 管理员登录
│       ├── me.php                     # [需登录] 用户信息 / 登出
│       ├── messages.php               # [需登录] 留言CRUD + 批量更新
│       ├── keys.php                   # [仅管理员] API Key 管理
│       ├── users.php                  # [仅管理员] 用户管理
│       ├── status.php                 # [仅管理员] 状态字典维护
│       └── export.php                 # [需登录] CSV导出
│
├── admin/                             # 管理后台前端（SPA）
│   ├── login.html                     # 登录页
│   ├── index.html                     # 留言列表（管理首页）
│   ├── keys.html                      # API Key 管理页
│   ├── users.html                     # 用户管理页
│   └── assets/
│       ├── app.js                     # 前端引擎 (~30KB)
│       └── style.css                  # 公共样式 (~14KB)
│
├── public/
│   └── embed-demo.html                # 留言提交 Demo 页
│
├── sql/
│   └── init.sql                       # 建表脚本 + 初始数据
│
└── data/                              # 运行时数据（.htaccess 保护）
    ├── messages.db                    # SQLite 数据库
    └── logs/                          # 日志目录（.htaccess 保护）
        └── app_YYYY-MM-DD.log
```

---

## 三、数据库设计（7 张表）

### 3.1 messages — 留言主表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| name | TEXT NOT NULL | 客户姓名 |
| phone | TEXT NOT NULL | 手机号 |
| email | TEXT DEFAULT NULL | 邮箱 |
| remark | TEXT DEFAULT NULL | 留言内容 |
| source | TEXT NOT NULL | 来源名称（= api_keys.site_name） |
| api_key | TEXT NOT NULL | 调用方API Key |
| ip_address | TEXT DEFAULT NULL | 提交者IP |
| user_agent | TEXT DEFAULT NULL | 浏览器UA |
| status | INTEGER DEFAULT 0 | 状态码 → status_dict.code |
| handle_record | TEXT DEFAULT NULL | 处理记录 |
| handler | TEXT DEFAULT NULL | 指派处理人 |
| created_at | TEXT | 提交时间（datetime） |
| handled_at | TEXT DEFAULT NULL | 处理时间 |
| handled_by | TEXT DEFAULT NULL | 处理人 |

索引：`idx_source`, `idx_status`, `idx_created_at`, `idx_handler`

### 3.2 api_keys — API Key 管理

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增 |
| api_key | TEXT UNIQUE NOT NULL | API密钥 |
| site_name | TEXT NOT NULL | 站点名称 |
| is_active | INTEGER DEFAULT 1 | 1=启用 0=禁用 |
| created_at | TEXT | 创建时间 |

不再内置默认 API Key；请在后台按站点单独创建。

### 3.3 status_dict — 状态字典

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增 |
| code | INTEGER UNIQUE NOT NULL | 状态码 |
| name | TEXT NOT NULL | 状态名称 |
| color | TEXT DEFAULT '#6B7280' | 显示颜色 |
| sort_order | INTEGER DEFAULT 0 | 排序 |
| is_active | INTEGER DEFAULT 1 | 软删除标记 |

初始数据：0=新留言, 1=已联系, 2=已成交, 3=无效

### 3.4 users — 用户表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增（ID=1 受保护不可删除） |
| username | TEXT UNIQUE NOT NULL | 用户名 |
| password | TEXT NOT NULL | bcrypt 加密 |
| role | TEXT DEFAULT 'user' | admin / user |
| real_name | TEXT DEFAULT NULL | 真实姓名 |
| is_active | INTEGER DEFAULT 1 | 状态 |

默认管理员：`admin`；当前部署已改为随机强密码（bcrypt），首次登录后请立即改成自管密码。

### 3.5 user_sources — 用户来源权限

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增 |
| user_id | INTEGER FK → users.id | 用户ID |
| source | TEXT NOT NULL | 允许查看的来源 |

普通用户只能看被分配的来源留言；管理员（role=admin）不受此限制。

### 3.6 sessions — Token 会话

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增 |
| user_id | INTEGER FK → users.id | 用户ID |
| token | TEXT UNIQUE NOT NULL | 32字节随机Token |
| ip_address | TEXT DEFAULT NULL | 登录IP |
| user_agent | TEXT DEFAULT NULL | 登录UA |
| expires_at | TEXT NOT NULL | 过期时间（24小时） |
| created_at | TEXT | 登录时间 |

登录时旧会话自动删除（单点登录），Token 有效期 24 小时。

### 3.7 rate_limits — 频率限制日志

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增 |
| ip_address | TEXT NOT NULL | 请求IP |
| action_type | TEXT DEFAULT 'submit' | 操作类型 |
| phone | TEXT DEFAULT '' | 手机号 |
| created_at | TEXT | 记录时间 |

索引：`idx_rate_ip`, `idx_rate_time`

---

## 四、核心类设计

### 4.1 Database — 数据库封装（单例）

```php
class Database {
    // 单例模式，私有构造
    public static function getInstance(): self

    // 初始化表（读取 init.sql）
    public function initTables(): void

    // 底层 PDO 实例
    public function getPdo(): PDO

    // 查询多条
    public function query(string $sql, array $params = []): array

    // 查询单条
    public function queryOne(string $sql, array $params = []): ?array

    // 执行写入
    public function execute(string $sql, array $params = []): int

    // 最后插入ID
    public function lastInsertId(): string
}
```

关键特性：
- PDO 异常模式 + WAL 模式 + 外键约束 + UTF-8
- 全部使用 `:named` 命名参数（统一风格）
- `initTables()` 仅在表不存在时创建（已存在的表跳过）

### 4.2 Auth — 认证模块

```php
class Auth {
    // API Key 校验（公开接口使用）
    public static function validateApiKey(string $apiKey): array|false

    // 用户登录（返回 token + 用户数据）
    public static function login(string $username, string $password): array

    // 退出登录
    public static function logout(string $token): void

    // 从请求头获取当前用户（Token 优先，Session 回退）
    public static function getAuthUser(): array|false

    // 强制登录检查（未登录返回401）
    public static function requireLogin(): array

    // 强制管理员检查（非admin返回403）
    public static function requireAdmin(): array

    // 获取用户可看的来源列表（admin=null全部，user=受限列表）
    public static function getUserSources(array $user): ?array

    // 获取客户端真实IP
    private static function getClientIP(): string
}
```

### 4.3 Response — 统一响应

```php
class Response {
    // 成功响应：{ code: 0, msg: "xxx", data: {...} }
    public static function success($data = null, string $msg = 'ok', int $httpCode = 200): void

    // 错误响应：{ code: xxx, msg: "xxx" }
    public static function error(int $code, string $msg, int $httpCode = 400): void

    // 分页响应：{ code: 0, data: { items:[], total, page, page_size } }
    public static function paginate(array $items, int $total, int $page, int $pageSize): void

    // CORS 预检处理
    public static function handleCors(): void
}
```

### 4.4 Validator — 参数校验

| 校验项 | 规则 | 错误码 |
|--------|------|--------|
| 姓名 | 必填，≤50字符 | 1001 |
| 手机号 | 必填，正则 `/^1[3-9]\d{9}$/` | 1002 |
| 邮箱 | 可选，`FILTER_VALIDATE_EMAIL` | 1003 |
| 留言 | ≤2000字符 | 1005 |
| API Key | 必填 | 1001 |
| XSS 防护 | `htmlspecialchars()` 递归处理 | — |

### 4.5 RateLimiter — 频率限制

| 限制类型 | 窗口 | 阈值 | 常量 |
|----------|------|------|------|
| 单IP频率 | 60秒 | 10次 | `RATE_LIMIT_WINDOW`, `RATE_LIMIT_MAX` |
| 重复提交 | 60秒 | 同IP+同手机 | `DUPLICATE_WINDOW` |

IP 获取支持：`X-Forwarded-For` → `X-Real-IP` → `REMOTE_ADDR`，含 `FILTER_VALIDATE_IP` 校验。

### 4.6 Logger — 日志

```php
class Logger {
    // 普通日志
    public static function log(string $action, array $data = [], string $level = 'INFO'): void

    // 错误日志
    public static function error(string $action, array $data = []): void
}
```

- 文件：`data/logs/app_YYYY-MM-DD.log`
- 格式：JSON行（timestamp, level, action, ip, data）
- 容错：写入失败不中断业务流程

---

## 五、API 接口清单

### 5.1 公开接口（无需 Token）

| 方法 | 路径 | 功能 | 主要错误码 |
|------|------|------|------------|
| POST | `/api/message.php` | 提交留言 | 1000~1005, 2000~2003, 5000 |
| POST | `/api/admin/login.php` | 管理员登录 | 401 |

### 5.2 需登录接口

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api/admin/me.php` | 当前用户信息 |
| GET | `/api/admin/me.php?action=logout` | 退出登录 |
| GET | `/api/admin/messages.php` | 留言列表（分页+筛选） |
| GET | `/api/admin/messages.php?id=X` | 留言详情 |
| PUT | `/api/admin/messages.php` | 更新留言（状态/处理人/记录） |
| POST | `/api/admin/messages.php` | 批量更新状态 |
| GET | `/api/admin/export.php` | CSV 导出 |

### 5.3 仅管理员接口

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api/admin/keys.php` | API Key 列表 |
| POST | `/api/admin/keys.php` | 新增 Key |
| PUT | `/api/admin/keys.php` | 启用/禁用 Key |
| DELETE | `/api/admin/keys.php` | 删除 Key |
| GET | `/api/admin/users.php` | 用户列表 |
| POST | `/api/admin/users.php` | 新增用户 |
| PUT | `/api/admin/users.php` | 编辑用户 |
| DELETE | `/api/admin/users.php` | 删除用户 |
| GET | `/api/admin/status.php` | 状态字典列表 |
| POST | `/api/admin/status.php` | 新增状态 |
| PUT | `/api/admin/status.php` | 编辑状态 |
| DELETE | `/api/admin/status.php` | 删除状态（软删） |

### 5.4 留言列表筛选参数

| 参数 | 说明 |
|------|------|
| `page` | 页码，默认1 |
| `page_size` | 每页条数，1-100，默认20 |
| `keyword` | 模糊搜索：姓名/手机/邮箱/内容/处理人 |
| `source` | 来源精确筛选 |
| `status` | 状态码筛选 |
| `handler` | 处理人模糊搜索 |
| `date_from` | 开始日期 |
| `date_to` | 结束日期 |

---

## 六、前端架构

### 6.1 页面清单

| 页面 | 文件 | 功能 |
|------|------|------|
| 登录页 | `admin/login.html` | 用户名密码登录 |
| 留言列表 | `admin/index.html` | 统计卡片 + 筛选 + 分页表格 + 详情弹窗 |
| Key 管理 | `admin/keys.html` | 增/启用/禁用/删 |
| 用户管理 | `admin/users.html` | 增/改/删 + 来源权限 |
| Demo 页 | `public/embed-demo.html` | 对外接入示例 |

### 6.2 app.js 核心逻辑

- **Token 管理**：`localStorage` 存储，`apiFetch()` 自动注入 `Authorization: Bearer`
- **路由分发**：`isPage(fragment)` 判断当前页面加载对应逻辑
- **角色权限**：根据 `userInfo.role` 动态渲染侧边栏菜单
- **日期筛选**：参数统一为 `date_from` / `date_to`
- **批量更新**：单次 POST `{ids, status}` 替代原来逐条 PUT
- **状态字典**：弹窗式 CRUD，含颜色选择器
- **CSV 导出**：`apiFetch` + Blob 下载（支持 Token 认证）

### 6.3 style.css 设计

- 侧边栏 240px 固定 + 右侧 flex 主区域
- 响应式断点：1024px / 768px / 480px
- 统一的 Badge / Button / Table / Pagination / Modal / Toast 组件样式
- 暗色侧边栏渐变背景

---

## 七、安全机制总览

| # | 措施 | 实现 |
|---|------|------|
| 1 | API Key 鉴权 | 每个站点独立 Key，可启用/禁用 |
| 2 | 密码加密 | `password_hash()` + BCRYPT |
| 3 | Token 认证 | 32字节随机值，24小时过期，单点登录 |
| 4 | 角色权限 | admin 全部 / user + user_sources 来源限制 |
| 5 | 频率限制 | 单IP × 60秒 × 10次 |
| 6 | 防重复提交 | 同IP + 同手机 × 60秒 × 1次 |
| 7 | SQL 注入防护 | 100% PDO 预处理语句（:named 参数） |
| 8 | XSS 防护 | `htmlspecialchars()` 递归过滤 + 前端 `esc()` |
| 9 | CORS 管控 | 统一 `Response::handleCors()`, 常量 `CORS_ORIGINS` |
| 10 | 文件保护 | .htaccess 禁止 data/ src/ config/ sql/ 目录及 .db/.log 文件访问 |
| 11 | Session 安全 | httponly + use_only_cookies |
| 12 | 操作日志 | JSON 格式日志，记录登录/提交/更新/批量操作 |

---

## 八、v1.0 优化记录（一次交付）

以下为 2026-07-24 完成的 17 项修复优化：

| # | 类型 | 问题 | 修复 |
|---|------|------|------|
| 1 | 🔴 | 前后端日期参数名不一致（`start_date` vs `date_from`） | 统一为 `date_from` / `date_to` |
| 2 | 🔴 | 留言详情接口（`messages.php?id=X`）缺失 | GET 处理器新增单条查询 |
| 3 | 🔴 | `deleteStatusDict` 请求方法/URL/认证三方全错 | `apiFetch` + `DELETE` + URL param |
| 4 | 🔴 | `batchUpdate` 逐条 PUT，未用后端批量接口 | 改为单次 POST `{ids, status}` |
| 5 | 🔴 | 7个 admin API 各自手写 CORS 代码 | 统一 `Response::handleCors()` |
| 6 | 🟡 | CORS 头重复输出（Response.json + 手写） | 消除重复 |
| 7 | 🟡 | Demo 页未传 `source` 字段 | JS 提交 + 示例代码均加入 |
| 8 | 🟡 | RateLimiter SQL 参数混用 `?` / `:named` | 全部统一为 `:named` |
| 9 | 🟡 | init.sql 缺少 `rate_limits` 表 | 补全建表 + 索引 |
| 10 | 🟡 | Session 插入未记 IP/UA | `getClientIP()` + INSERT 写入 |
| 11 | 🟢 | `Validator::xss()` 已定义但未调用 | message.php 接收数据后调用 |
| 12 | 🟢 | message.php SQL 用 `?` 占位符 | 改为 `:named` 统一风格 |
| 13 | 🟢 | messages 表 `website` 字段从未使用 | 从 init.sql 移除 |
| 14 | 🟢 | 根目录暴露测试脚本 | 删除 `test_api.php` / `_api_test3.php` |
| 15 | 🟢 | `check.php` 引用已删除文件 | 清理引用 + 补全表检查 |
| 16 | 🟢 | config 中 `SESSION_LIFETIME` 冗余 | 删除无用常量 |
| 17 | 🟢 | 缺少请求日志 | 新建 `Logger.php`，集成登录/提交/更新 |

---

## 九、数据流图

```
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  官网 sanqicn │    │  阿里店铺     │    │  其他来源     │
│  (前端表单)   │    │  (前端表单)   │    │  (前端表单)   │
└──────┬───────┘    └──────┬───────┘    └──────┬───────┘
       │                   │                   │
       └───────────────────┼───────────────────┘
                           │ POST /api/message.php
                           │ { api_key, name, phone, email, remark, source }
                           ▼
       ┌───────────────────────────────────────────────┐
       │              api/message.php                   │
       │  ① Response::handleCors()  → CORS 预检         │
       │  ② JSON 解析 + Validator::xss()                 │
       │  ③ Database::initTables() → 自动建表           │
       │  ④ Validator::validate()  → 参数校验           │
       │  ⑤ Auth::validateApiKey()  → 鉴权              │
       │  ⑥ RateLimiter::check()    → 频率限制          │
       │  ⑦ RateLimiter::checkDuplicate() → 防重复      │
       │  ⑧ INSERT messages + Logger::log()              │
       │  ⑨ Response::success()  → JSON 响应            │
       └───────────────────┬───────────────────────────┘
                           │
                           ▼
       ┌───────────────────────────────────────────────┐
       │            SQLite (data/messages.db)           │
       │  messages | api_keys | users | user_sources    │
       │  sessions | status_dict | rate_limits          │
       └───────────────────┬───────────────────────────┘
                           │
                           ▼
       ┌───────────────────────────────────────────────┐
       │           管理后台 (admin/*.html)               │
       │  登录 → Token/Session ← Auth::login()          │
       │  留言列表 → GET /api/admin/messages.php        │
       │  筛选/分页/详情/状态更新/批量操作/CSV导出       │
       │  Key管理/用户管理/状态字典 ← 仅管理员           │
       └───────────────────────────────────────────────┘
```

---

## 十、部署信息

| 项目 | 值 |
|------|------|
| 环境要求 | PHP ≥ 7.4 + PDO + PDO_SQLite + JSON |
| 启动命令 | `php -S 0.0.0.0:8080 -t e:/CEODEXPro/ReciveMessage` |
| 管理后台 | `http://localhost:8080/admin/login.html` |
| 管理员账号 | `admin` / 随机强密码（首次登录后请修改） |
| API 地址 | `http://localhost:8080/api/message.php` |
| Demo 页 | `http://localhost:8080/public/embed-demo.html` |
| 检查工具 | `http://localhost:8080/check.php` |
| 默认 API Key | 无；请在后台按站点创建 |

---

## 十一、已知局限与后续建议

| # | 问题 | 建议 |
|---|------|------|
| 1 | SQLite 写锁全库级，高并发瓶颈 | 日均超万条迁移 MySQL/PostgreSQL |
| 2 | 频率限制基于 SQLite，并发性能差 | 迁移 Redis 实现 |
| 3 | 日志无自动轮转清理 | 增加 cron 任务定期清理 |
| 4 | Demo 页 API Key 硬编码前端 | 生产环境改用后端转发 |
| 5 | Token 过期仅登录时清理 | 增加定时清理或懒清理 |
| 6 | 手机号仅支持中国大陆格式 | 扩展国际号码支持 |
| 7 | 无 CSRF Token | 管理后台考虑增加 |
| 8 | 无微信/邮件通知 | 可加 webhook 通知 |

---

> **文档状态：✅ 与实际代码完全一致**  
> 基于 2026-07-24 全部 17 项优化完成后的代码快照生成。
